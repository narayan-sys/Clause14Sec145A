# Static assets only — there is no Worker script. The files in this
# repository are served directly, except those listed in .assetsignore.

name = "clause14sec145a"
compatibility_date = "2026-08-09"

[assets]
directory = "."
not_found_handling = "404-page"
html_handling = "auto-trailing-slash"
